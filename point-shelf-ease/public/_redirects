# Redirect rules for SPA (Single Page Application) routing
# All non-file requests should be redirected to index.html for client-side routing

# API routes (if any backend routes need to be preserved)
/api/* /api/:splat 200

# Static assets should be served directly (already handled by Cloudflare)
/assets/* /assets/:splat 200
/favicon.ico /favicon.ico 200
/robots.txt /robots.txt 200
# All other requests should go to index.html for SPA routing
/* /index.html 200
